apiVersion: v1
kind: ServiceAccount
metadata:
  name: vllm-server
  namespace: act6-serve
  # The vLLM workload needs no GCP credentials at runtime.
  # Model pulls from HuggingFace Hub; Pod-snapshot GCS access is via the
  # gkenode service agent (granted in infra/09-serving-iam.sh), not this KSA.
